(()=>{'use strict';
const KEY='concorsoQuestRT.oral.v1';
const PASS=18;
const CRITERIA=[
  ['completezza','Completezza','Copre i punti essenziali della traccia senza omissioni rilevanti.'],
  ['ordine','Ordine e chiarezza','Espone una sequenza logica: inquadramento, verifiche, intervento, verifica finale.'],
  ['precisione','Precisione tecnica','Usa terminologia corretta, distingue concetti e propone azioni tecnicamente coerenti.'],
  ['sicurezza','Sicurezza','Considera minimo privilegio, protezione dati, evidenze, impatto e misure di sicurezza quando pertinenti.'],
  ['troubleshooting','Troubleshooting','Parte da evidenze, formula ipotesi verificabili, isola la causa e chiude con prevenzione/documentazione.']
];
const LABELS=['Assente','Gravemente insufficiente','Insufficiente','Parziale','Adeguato','Molto buono','Eccellente'];
let timer=null,startedAt=0,current=null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function read(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch{return []}}
function write(x){localStorage.setItem(KEY,JSON.stringify(x.slice(0,100)))}
function prompts(){return Array.isArray(window.CONCORSO_ORAL_PROMPTS)?window.CONCORSO_ORAL_PROMPTS:[]}
function stage(){return document.getElementById('gameStage')}
function stopTimer(){if(timer){clearInterval(timer);timer=null}}
function fmt(sec){sec=Math.max(0,sec);return `${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`}
function injectStyle(){if(document.getElementById('oralEvalStyle'))return;const s=document.createElement('style');s.id='oralEvalStyle';s.textContent=`
.oral-score-grid{display:grid;gap:.8rem;margin:1rem 0}.oral-score-row{padding:.85rem;border:1px solid var(--line);border-radius:14px;background:#fff}.oral-score-head{display:flex;justify-content:space-between;gap:1rem;align-items:center}.oral-score-row input[type=range]{width:100%;margin:.7rem 0 .25rem}.oral-score-value{min-width:4.4rem;text-align:center;padding:.25rem .5rem;border-radius:999px;background:#f7e7ec;color:var(--red);font-weight:900}.oral-score-desc{color:var(--muted);font-size:.78rem}.oral-summary{display:grid;grid-template-columns:repeat(4,1fr);gap:.5rem;margin:.8rem 0}.oral-summary .stat{padding:.7rem .35rem}.oral-history{display:grid;gap:.5rem;margin-top:.8rem}.oral-history-item{padding:.7rem;border-left:4px solid var(--line);border-radius:10px;background:#fff}.oral-history-item.pass{border-left-color:var(--ok)}.oral-history-item.fail{border-left-color:var(--bad)}.oral-actions{display:flex;gap:.6rem;flex-wrap:wrap}.oral-actions>*{flex:1;min-width:150px}.oral-result-score{font-size:2rem;font-weight:900;color:var(--red)}@media(max-width:560px){.oral-summary{grid-template-columns:repeat(2,1fr)}}`;
document.head.appendChild(s)}
function historyHtml(limit=5){const h=read();if(!h.length)return '<p class="privacy-note">Nessuna simulazione orale registrata.</p>';return `<div class="oral-history">${h.slice(0,limit).map(x=>`<div class="oral-history-item ${x.total>=PASS?'pass':'fail'}"><strong>${x.total}/30 · ${esc(x.topic)}</strong><br><small>${new Date(x.date).toLocaleString('it-IT')} · ${x.total>=PASS?'soglia raggiunta':'sotto soglia'} · risposta ${fmt(x.secondsUsed||0)}</small></div>`).join('')}</div>`}
function summaryHtml(){const h=read(),n=h.length,avg=n?(h.reduce((a,x)=>a+x.total,0)/n).toFixed(1):'–',best=n?Math.max(...h.map(x=>x.total)):'–',passed=h.filter(x=>x.total>=PASS).length;return `<div class="oral-summary"><article class="stat"><span>🎙️ Simulazioni</span><strong>${n}</strong><small>registrate</small></article><article class="stat"><span>📊 Media</span><strong>${avg}</strong><small>/30</small></article><article class="stat"><span>🏆 Migliore</span><strong>${best}</strong><small>/30</small></article><article class="stat"><span>✅ ≥18/30</span><strong>${passed}</strong><small>superate</small></article></div>`}
function refreshProfile(){injectStyle();const profile=document.getElementById('view-profile');if(!profile)return;let panel=document.getElementById('oralHistoryPanel');if(!panel){panel=document.createElement('section');panel.id='oralHistoryPanel';panel.className='panel';const before=profile.querySelector('.action-stack');before?profile.insertBefore(panel,before):profile.appendChild(panel)}panel.innerHTML=`<div class="panel-heading"><div><p class="eyebrow">Preparazione orale</p><h3>Cronologia simulazioni</h3></div><span class="status-pill">soglia ${PASS}/30</span></div>${summaryHtml()}${historyHtml(8)}<div class="oral-actions" style="margin-top:.8rem"><button id="oralExportHistory" class="secondary-btn" type="button">Esporta cronologia</button><button id="oralClearHistory" class="ghost-btn" type="button">Azzera cronologia orale</button></div>`;
const ex=document.getElementById('oralExportHistory');if(ex)ex.onclick=exportHistory;const cl=document.getElementById('oralClearHistory');if(cl)cl.onclick=()=>{if(confirm('Azzero tutta la cronologia delle simulazioni orali?')){localStorage.removeItem(KEY);refreshProfile()}}}
function renderPrompt(){injectStyle();stopTimer();const ps=prompts(),el=stage();if(!el)return;if(!ps.length){el.classList.remove('hidden');el.innerHTML='<h3>Arena orale</h3><p>Nessuna traccia disponibile.</p>';return}current=ps[Math.floor(Math.random()*ps.length)];startedAt=Date.now();let sec=180;el.classList.remove('hidden');el.innerHTML=`<h3>🎙️ Arena orale · ${esc(current[0])}</h3><p class="oral-prompt">${esc(current[1])}</p><div class="oral-timer" id="oralTimer">03:00</div><p><strong>Consegna:</strong> rispondi come davanti alla commissione. Hai 3 minuti; struttura la risposta e motiva le priorità.</p><details><summary>Criteri tecnici della traccia</summary><ol>${(current[2]||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol></details><div class="oral-actions"><button id="oralEvaluate" class="primary-btn" type="button">Termina e valuta</button><button id="oralSkip" class="secondary-btn" type="button">Nuova traccia</button></div>${summaryHtml()}${historyHtml(3)}`;
const tick=()=>{sec--;const t=document.getElementById('oralTimer');if(!t){stopTimer();return}t.textContent=fmt(sec);if(sec<=0){stopTimer();t.textContent='00:00 · tempo scaduto'}};timer=setInterval(tick,1000);document.getElementById('oralEvaluate').onclick=renderEvaluation;document.getElementById('oralSkip').onclick=renderPrompt}
function renderEvaluation(){stopTimer();const el=stage();if(!el||!current)return;const elapsed=Math.min(180,Math.round((Date.now()-startedAt)/1000));el.innerHTML=`<h3>📝 Autovalutazione · ${esc(current[0])}</h3><p>${esc(current[1])}</p><p>Assegna <strong>0–6 punti</strong> a ciascun criterio. Totale massimo 30; la soglia di riferimento dell'orale è <strong>${PASS}/30</strong>.</p><div class="oral-score-grid">${CRITERIA.map(([id,name,desc])=>`<div class="oral-score-row"><div class="oral-score-head"><div><strong>${esc(name)}</strong><div class="oral-score-desc">${esc(desc)}</div></div><span id="score-${id}" class="oral-score-value">3 · ${LABELS[3]}</span></div><input data-oral-score="${id}" type="range" min="0" max="6" step="1" value="3" aria-label="${esc(name)}"></div>`).join('')}</div><div class="panel-heading"><strong>Punteggio provvisorio</strong><span id="oralTotal" class="status-pill">15/30</span></div><div class="oral-actions"><button id="oralSaveScore" class="primary-btn" type="button">Salva valutazione</button><button id="oralBackPrompt" class="secondary-btn" type="button">Ripeti la traccia</button></div>`;
const update=()=>{let total=0;document.querySelectorAll('[data-oral-score]').forEach(i=>{const v=+i.value;total+=v;const o=document.getElementById(`score-${i.dataset.oralScore}`);if(o)o.textContent=`${v} · ${LABELS[v]}`});document.getElementById('oralTotal').textContent=`${total}/30`};document.querySelectorAll('[data-oral-score]').forEach(i=>i.oninput=update);document.getElementById('oralBackPrompt').onclick=renderPrompt;document.getElementById('oralSaveScore').onclick=()=>saveScore(elapsed)}
function saveScore(secondsUsed){const scores={},el=stage();document.querySelectorAll('[data-oral-score]').forEach(i=>scores[i.dataset.oralScore]=+i.value);const total=Object.values(scores).reduce((a,b)=>a+b,0),entry={id:Date.now(),date:new Date().toISOString(),topic:current[0],prompt:current[1],secondsUsed,scores,total};const h=read();h.unshift(entry);write(h);refreshProfile();el.innerHTML=`<div class="result-card"><span class="result-emoji">${total>=PASS?'🏆':'📚'}</span><div class="oral-result-score">${total}/30</div><h3>${total>=PASS?'Soglia raggiunta':'Da consolidare'}</h3><p>${total>=PASS?`Hai raggiunto la soglia di ${PASS}/30.`:`Sei sotto la soglia di ${PASS}/30: individua i criteri più bassi e ripeti la traccia.`}</p></div><div class="oral-score-grid">${CRITERIA.map(([id,n])=>`<div class="oral-score-row"><div class="oral-score-head"><strong>${esc(n)}</strong><span class="oral-score-value">${scores[id]}/6</span></div></div>`).join('')}</div><div class="oral-actions"><button id="oralNewAfterScore" class="primary-btn" type="button">Nuova traccia</button><button id="oralRepeatAfterScore" class="secondary-btn" type="button">Ripeti questa traccia</button></div>${summaryHtml()}${historyHtml(5)}`;document.getElementById('oralNewAfterScore').onclick=renderPrompt;document.getElementById('oralRepeatAfterScore').onclick=()=>{startedAt=Date.now();renderPromptWithCurrent()}}
function renderPromptWithCurrent(){injectStyle();stopTimer();const el=stage();if(!el||!current)return;startedAt=Date.now();let sec=180;el.classList.remove('hidden');el.innerHTML=`<h3>🎙️ Arena orale · ${esc(current[0])}</h3><p class="oral-prompt">${esc(current[1])}</p><div class="oral-timer" id="oralTimer">03:00</div><p><strong>Secondo tentativo:</strong> prova a migliorare soprattutto i criteri che avevano il punteggio più basso.</p><details><summary>Criteri tecnici della traccia</summary><ol>${(current[2]||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ol></details><div class="oral-actions"><button id="oralEvaluate" class="primary-btn" type="button">Termina e valuta</button><button id="oralSkip" class="secondary-btn" type="button">Nuova traccia</button></div>`;timer=setInterval(()=>{sec--;const t=document.getElementById('oralTimer');if(!t)return stopTimer();t.textContent=fmt(sec);if(sec<=0){stopTimer();t.textContent='00:00 · tempo scaduto'}},1000);document.getElementById('oralEvaluate').onclick=renderEvaluation;document.getElementById('oralSkip').onclick=renderPrompt}
function exportHistory(){const h=read(),blob=new Blob([JSON.stringify({exportedAt:new Date().toISOString(),threshold:PASS,criteria:CRITERIA.map(x=>x[1]),history:h},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`concorso-quest-rt-orale-${new Date().toLocaleDateString('en-CA')}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
window.CONCORSO_ORAL_ARENA={start:renderPrompt,refreshProfile,history:read,exportHistory};
injectStyle();
})();
